---
title: "EvalForge"
slug: "evalforge"
tagline: "Unified evaluation pipeline for generative video and agent systems"
description: "Open-source Python framework for end-to-end evaluation of T2V/I2V models and conversational agents — async generation, VBench metrics, LLM classification, and automated reporting."
date: 2025-10-01
endDate: 2026-03-01
status: "completed"
featured: true
techStack: ["Python", "asyncio", "VBench", "Pydantic", "Click", "Next.js"]
highlights:
  - "Unified 5-stage pipeline: classification → generation → evaluation → analysis → reporting"
  - "Async batch orchestrator with N-run retry, crash recovery, and per-provider concurrency control"
  - "12 VBench metrics for video quality; 5-dimension LLM judge for agent assessment"
  - "Config-driven plugin architecture with registries for providers, metrics, and report formats"
links:
  github: "https://github.com/zzzhhn/evalforge"
  live: "https://evaluation.bobbyzhong.com"
order: 1
---

import EvalVideoDashboard from "../../components/react/EvalVideoDashboard.tsx";
import EvalAgentDashboard from "../../components/react/EvalAgentDashboard.tsx";
import MermaidDiagram from "../../components/react/MermaidDiagram.tsx";

Evaluating generative AI models is fragmented by default: T2V pipelines, agent test harnesses, and quality judges are built in isolation, making cross-model comparisons brittle and results hard to reproduce. EvalForge replaces that with a single config-driven pipeline that classifies prompts, orchestrates generation at scale, runs standardised metrics, and produces structured reports automatically — across both video generation and conversational agents.

## Pipeline

<div className="eval-pipeline">
  <div className="eval-stage">
    <div className="eval-stage-num">Stage 1</div>
    <div className="eval-stage-name">Classify</div>
    <div className="eval-stage-detail">LLM prompt / intent taxonomy with content-hash cache</div>
  </div>
  <div className="eval-stage">
    <div className="eval-stage-num">Stage 2</div>
    <div className="eval-stage-name">Generate</div>
    <div className="eval-stage-detail">Async batch · per-provider semaphore · N-run retry</div>
  </div>
  <div className="eval-stage">
    <div className="eval-stage-num">Stage 3</div>
    <div className="eval-stage-name">Evaluate</div>
    <div className="eval-stage-detail">VBench metrics (video) · LLM-as-judge (agent)</div>
  </div>
  <div className="eval-stage">
    <div className="eval-stage-num">Stage 4</div>
    <div className="eval-stage-name">Analyze</div>
    <div className="eval-stage-detail">Statistical grading · baseline comparison · cross-model ranking</div>
  </div>
  <div className="eval-stage">
    <div className="eval-stage-num">Stage 5</div>
    <div className="eval-stage-name">Report</div>
    <div className="eval-stage-detail">JSON / DOCX / HTML with radar charts, heatmaps</div>
  </div>
</div>

### T2V Evaluation Track

<MermaidDiagram client:visible chart={`flowchart LR
    input([Prompt Dataset]):::io --> s1

    s1["Stage 1: Classify\\nLLM taxonomy · hash cache\\n7 prompt categories"] --> s2
    s2["Stage 2: Generate\\nasync · per-provider semaphore\\nN-run retry · checkpoint"] --> s3
    s3["Stage 3: Evaluate\\nVBench 1.0 · 16 metrics\\n+ VBench++ I2V dims"] --> s4
    s4["Stage 4: Analyze\\npercentile normalise\\ncross-model ranking"] --> s5
    s5["Stage 5: Report\\nJSON · DOCX · HTML"]:::out --> output([Interactive Dashboard]):::io

    providers([Model Providers\\nKling · Veo · Runway · Pika]):::ext --> s2
    vbench([VBench Evaluator\\noptional GPU server]):::ext --> s3

    classDef io fill:#6366f11a,stroke:#6366f1,color:#a5b4fc
    classDef ext fill:#10b9811a,stroke:#10b981,color:#6ee7b7
    classDef out fill:#f59e0b1a,stroke:#f59e0b,color:#fcd34d`} caption="EvalForge T2V track — text-to-video evaluation pipeline" />

### Agent Evaluation Track

<MermaidDiagram client:visible chart={`flowchart LR
    input([Conversations\\n500 sessions · JSONL]):::io --> s1

    s1["Stage 1: Classify Intent\\n7 categories\\nhybrid rules + LLM"] --> s3
    s3["Stage 3: LLM Judge\\n5 dimensions\\n5-shot calibration"] --> s4
    s4["Stage 4: Aggregate\\nturn → session\\n→ intent category"] --> s5
    s5["Stage 5: Report\\nheatmaps · radar · JSON"]:::out --> output([Intent + Quality Profiles]):::io

    llm([LLM Backend\\nGemma 4 local or\\nOpenAI-compat API]):::ext --> s1
    llm --> s3

    classDef io fill:#6366f11a,stroke:#6366f1,color:#a5b4fc
    classDef ext fill:#10b9811a,stroke:#10b981,color:#6ee7b7
    classDef out fill:#f59e0b1a,stroke:#f59e0b,color:#fcd34d`} caption="EvalForge Agent track — Stage 2 (Generation) is skipped when conversations are pre-collected" />

## Engineering Highlights

- **Plugin Registry** — Decorator-driven `Registry[T]` for providers, metrics, and reporters. New integrations register via `@PROVIDER_REGISTRY.register("name")` — no core changes required.
- **Crash Recovery** — JSON checkpoint persistence with atomic writes (`tempfile` + `os.rename`). Interrupted pipelines resume from the last completed case without re-running prior generations.
- **Async Orchestration** — `asyncio.Semaphore`-based per-provider concurrency prevents rate-limit cascade failures across parallel API calls. Configurable N-run averaging with exponential-backoff retry.
- **Dual LLM Backend** — Local open-source models (Gemma 4 via OpenAI-compatible server) and cloud APIs, with automatic fallback. Configured via `base_url` + `api_key` — no code changes to switch backends.
- **Professional CLI** — Click-based with 7 subcommands: `run`, `generate`, `evaluate`, `classify`, `report`, `validate`, `info`.

<div className="eval-section-divider">
  <h2 className="eval-section-title">Video Evaluation Track</h2>
  <p className="eval-section-desc">
    T2V models evaluated across 12 VBench dimensions (raw scores vary widely by metric type —
    quality metrics like Subject Consistency cluster at 95–99%, while semantic metrics like
    Spatial Relationship and Dynamic Degree spread broadly, which is expected and documented
    in the VBench paper). EvalForge computes composite dimension scores by normalising each
    metric to its observed model distribution, enabling fair cross-dimension comparison.
    Select models to compare · click tabs to inspect raw per-metric VBench values.
  </p>
</div>

<EvalVideoDashboard client:visible />

<div className="eval-section-divider">
  <h2 className="eval-section-title">Agent Evaluation Track</h2>
  <p className="eval-section-desc">
    500 conversations from an internal Gemma 4-based conversational assistant evaluated
    through EvalForge's agent track: intent classification (7 categories) and 5-dimension
    quality scoring via LLM-as-judge with 5-shot calibration — including a faithfulness
    dimension to quantify hallucination rate. Click an intent slice to drill down into
    per-category quality profiles.
  </p>
</div>

<EvalAgentDashboard client:visible />
